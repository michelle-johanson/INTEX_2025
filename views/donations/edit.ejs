<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>

    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <%- include("../partials/navbar") %>

        <div class="container mt-4" style="max-width: 600px;">

            <h1>Edit Donation</h1>

            <form action="/donations/<%= donation.id %>/edit" method="POST" class="mt-3">

                <div class="mb-3">
                    <label class="form-label">Donor Name</label>
                    <input type="text" name="donor" class="form-control" value="<%= donation.donor %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Amount ($)</label>
                    <input type="number" name="amount" class="form-control" min="1" value="<%= donation.amount %>"
                        required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Message</label>
                    <textarea name="message" class="form-control"><%= donation.message %></textarea>
                </div>

                <button class="btn btn-success">Save Changes</button>
            </form>

            <br>
            <a href="/donations" class="btn btn-secondary">Cancel</a>

        </div>

</body>

</html>