<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>

    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

    <%- include("../partials/navbar") %>

        <div class="container mt-4">

            <h1>Events</h1>

            <% if (session.access_level==="admin" ) { %>
                <a href="/events/new" class="btn btn-primary mb-3">Add Event</a>
                <% } %>

                    <ul class="list-group">
                        <% events.forEach(e=> { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span>
                                    <%= e.name %> — <%= e.date %>
                                </span>
                                <a href="/events/<%= e.id %>" class="btn btn-sm btn-outline-secondary">Details</a>
                            </li>
                            <% }) %>
                    </ul>

        </div>

</body>

</html>