<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include("../partials/navbar") %>

        <div class="container mt-4">

            <h1>Survey #<%= survey.id %>
            </h1>

            <p><strong>Participant:</strong>
                <%= participant.first_name %>
                    <%= participant.last_name %>
            </p>

            <p><strong>Event:</strong>
                <%= event.name %>
            </p>

            <p><strong>Rating:</strong>
                <%= survey.rating %> / 5
            </p>

            <p><strong>Comments:</strong>
                <%= survey.comments || "No comments" %>
            </p>

            <p><strong>Date Taken:</strong>
                <%= survey.date_taken %>
            </p>

            <% if (session.access_level==="admin" ) { %>
                <a href="/surveys/<%= survey.id %>/edit" class="btn btn-warning">Edit</a>

                <form action="/surveys/<%= survey.id %>/delete" method="POST" style="display:inline;">
                    <button class="btn btn-danger">Delete</button>
                </form>
                <% } %>

                    <br><br>
                    <a href="/surveys" class="btn btn-secondary">Back</a>

        </div>
</body>

</html>