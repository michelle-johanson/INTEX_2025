<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include("../partials/navbar") %>

        <div class="container mt-4">

            <h1>Surveys</h1>

            <% if (session.access_level==="admin" ) { %>
                <a href="/surveys/new" class="btn btn-primary mb-3">Add Survey</a>
                <% } %>

                    <ul class="list-group">
                        <% surveys.forEach(s=> { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span>
                                    Survey #<%= s.id %> — Rating: <%= s.rating %>
                                </span>
                                <a href="/surveys/<%= s.id %>" class="btn btn-outline-secondary btn-sm">
                                    Details
                                </a>
                            </li>
                            <% }) %>
                    </ul>

        </div>
</body>

</html>