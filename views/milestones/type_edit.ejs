<div class="page-container" style="max-width: 600px; margin: 0 auto;">

    <h1 class="page-title">Edit Milestone Type</h1>
    <p class="text-muted">Rename the title of this milestone across all participant records.</p>

    <form action="/milestones/type/<%= encodeURIComponent(oldTitle) %>/edit" method="POST" class="form">
        
        <label for="MilestoneTitle">Current Milestone Title</label>
        <input id="MilestoneTitle" 
               type="text" 
               name="MilestoneTitle" 
               value="<%= oldTitle %>" 
               placeholder="Enter new milestone title"
               required />

        <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
            <button type="submit" class="btn btn-primary">Save New Title</button>
            
            <a href="/milestones/type/<%= encodeURIComponent(oldTitle) %>" class="btn btn-secondary">Cancel</a>
        </div>
        
    </form>
</div>